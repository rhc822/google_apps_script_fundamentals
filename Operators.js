function mathOperators(){

    var a = 10;
    var b = 2;

    //standard operators + - * /
    Logger.log(a + b);
    Logger.log(a - b);
    Logger.log(a * b);
    Logger.log(a / b);

    //Raise to power of
    var c = Math.pow(a, b);
    Logger.log(c);

    var i = 0;
    Logger.log(i);

    //increment by 1
    i++;
    Logger.log(i);

    //reduce by 1
    i--;
    Logger.log(i);

   }


   function stringOperators() {

    //can use single or double quotes around strings Logger.log("Test")

   }
